# Probe log

- 2026-08-27 (M0/P1/P4): Git contained no recoverable pre-framework source, so the concrete control had to be reconstructed and frozen under `baseline/hand_rolled/`; comparisons explicitly use that artifact rather than a remembered implementation.
- 2026-08-27 (M0/P5): Current Pydantic AI 2.35 exposes MCP at `pydantic_ai.mcp` and AG-UI at `pydantic_ai.ui.ag_ui`, not the paths assumed by the plan; framework source inspection was needed to find the moved APIs.
- 2026-08-27 (M0/P2): `pydantic-ai-slim[mcp]` installs only FastMCP client support; an in-process server fails at import until full `fastmcp` (or its server extra) is added.
- 2026-08-27 (M1/P2): `MCPToolset(FastMCP)` made in-memory discovery and calls native to an `Agent` run with no transport process, but union return values arrive through the direct FastMCP client as a structured `result` root and need explicit typed unwrapping outside Pydantic AI.
- 2026-08-27 (M1/P5): PGlite socket accepts asyncpg query traffic but its COPY path loses protocol synchronization and asyncpg's graceful close waits indefinitely; deterministic tests use typed CSV inserts and terminate each test connection. This is test-harness/transport friction, not MCP behavior.
- 2026-08-27 (M1/P1): The deterministic walking slice needed one `Agent`, one filtered MCP toolset, and no hand-written tool dispatch; the control needs an explicit request loop, wire-message construction, retry limit, and tool-result plumbing.
- 2026-08-27 (M2/P1): Frozen output/query variants make truncation and rejection assertions direct and prevent status-string branching; provider final output is validated as `AgentAnswer`, at the cost of an output-tool round trip for tool-output mode.
- 2026-08-27 (M2/P2): Tool exceptions were less useful than typed safe rejection values for keeping backend details out of history. FastMCP schemas preserve the variants, while database error sanitization remains application code as expected.
- 2026-08-27 (M2/P5): `AgentRunResult.usage` is now a property rather than the older callable shape; source/signature inspection corrected the measurement seam.
- 2026-08-27 (M3/P2/P8): Native `FilteredToolset(MCPToolset(...))` made the three exposure shapes one small builder; prefetch still needed a separate FastMCP client call plus typed result parsing, the clearest point where in-process MCP felt bolted on.
- 2026-08-27 (M3/P8): The final real-Ollama v1 matrix had 18/18 correct runs and all safety/held-out checks green. Prefetched won lexicographically: 6 correct, 2.5 request macro-median, 14.229s latency, and 1654.5 input tokens versus catalog 3.5/15.629s/2197.5 and granular 5.5/20.872s/3720.
- 2026-08-27 (M3/P7): An early workload wording (“completed journeys”) caused repeat timeouts despite an equivalent direct smoke succeeding; making the v1 prompt concrete before freezing the artifact produced 18/18. Local-model reliability is sensitive to wording even when schema/tool correctness is unchanged.
- 2026-08-27 (M4/P3): The route still ends in one `AGUIAdapter.dispatch_request` call, but real behavior adds per-request catalog prefetch, request deps, model settings, filtered toolsets, and an `on_complete` bridge for structured output text/usage. The front door stayed recognizable, not literally one line of behavior.
- 2026-08-27 (M4/P4): The same real journey succeeded through both controls. The final artifact has AG-UI at 23 events across 9 observed types in 7.200s and raw SSE at 12 events across 4 observed types in 19.142s. The event count is vocabulary evidence, not a protocol-speed claim.
- 2026-08-27 (M4/P4/P5): Reconstructing valid AG-UI history required grouping tool calls by parent message, echoing call/result pairs, and preserving encrypted continuity events. Missing a call on an existing assistant message deterministically broke the next turn; source inspection of the adapter was needed to understand the wire invariant.
- 2026-08-27 (M4/P5/P7): Gemma reasoning exposed an AG-UI stream-state failure (`handle_thinking_start must be called before handle_thinking_delta`). Ollama's OpenAI endpoint ignored `think:false`; `reasoning_effort: none` suppressed reasoning reliably enough for the accepted journey and is now explicit app configuration.
- 2026-08-27 (M4/P6): Full resend met the probe's declared turn-10 gate: 4223/262144 input tokens (1.61%), 12.867s versus 6.747s at turn 1 (1.91×), and a correct answer. Turn 2 still failed and turn 8 took 105.87s, so “acceptable” is only the narrow threshold verdict, not a reliability endorsement.
- 2026-08-27 (M4/P6): `ProcessHistory` wiring was one capability argument plus a 34-line immutable pair-preserving processor. It cut serialized model history from 28,584 to 11,957 bytes and preserved correctness, but total turn input rose from 4,223 to 4,771 tokens and latency reached 132.5s because the model needed more work; compaction saved payload, not this run's cost.
- 2026-08-27 (M5/P7): The agent image built and a containerized agent + seeded Postgres acceptance path passed against the exact host Ollama model after one observed flaky AG-UI run. The installed Docker CLI has no Compose plugin and the 0.32.15 Ollama image pull stalled twice, so no claim is made that the checked-in full stack was executed here.
- 2026-08-27 (M5/P7): Gemma completed core, AG-UI, baseline, held-out, and hybrid-composed journeys; no SQL-only fallback was needed. Redundant discovery/query calls and latency outliers remain the practical local-model cost.
- 2026-08-27 (review/P4): Independent Terra review found that completion bridging duplicated a real answer and that browser argument accumulation always targeted the first parallel call. RED regressions now suppress the bridge when matching text already streamed and select argument targets by call ID.
- 2026-08-27 (review/safety): Independent review found the output cap was applied after `fetch()` materialized every row. A slow-row RED test now proves the asyncpg cursor stops after `row_cap + 1`, making truncation a resource cap rather than only a response cap.
- 2026-08-27 (final review/safety): Terra found that read-only SQL under Compose's bootstrap superuser still allowed privileged file/program capabilities. RED tests now reject non-query statement families and known server-file/admin functions before execution, while Compose provisions a dedicated `NOSUPERUSER` SELECT-only application role.
- 2026-08-27 (final review/resource): Terra found the app-scoped MCP trace retained every result indefinitely. `create_app` now explicitly disables and clears call recording; traces remain enabled only for bounded acceptance/experiment runs.
- 2026-08-27 (security re-gate): Terra found top-level keyword checks missed data-modifying CTEs and `EXPLAIN ANALYZE` writes. An unreachable-DSN RED test now proves recursive DML/DDL token scanning rejects both before any connection attempt.
