# Probe log

- 2026-09-08 (demo/model): A three-case development-only Gemma smoke run on the frozen large dataset scored 2/3: total count and morning imbalance passed, cohort retention hit the 90-second cap; the timeout is retained as a failure, not retried away, and no held-out model evaluation was used for tuning.

- 2026-09-08 (demo/data): The versioned large synthetic dataset uses SQL/MD5 rather than random state or checked-in bulk CSV; all 16 frozen reference tables are independently reproduced with standard-library Python, including retention, gaps-and-islands, and interpolated percentiles.
- 2026-09-08 (demo/holdout): PGlite socket clients share prepared-statement state: a RED test found verifier SQL discoverable through pg_prepared_statements even after closing clients; benchmark-only orchestration now DEALLOCATEs verifier statements before model runs, without changing application DB access.

- 2026-09-08 (frontend/assistant-ui): Followed the upstream with-ag-ui example using published packages rather than monorepo workspace dependencies; `useAgUiRuntime` plus `HttpAgent` owns SSE/history/cancellation, while React primitives work under Vite without Next.js.
- 2026-09-08 (frontend/testing): Playwright's sync API owns a running event loop, so the page fixture explicitly depends on async PGlite seeding to avoid pytest-asyncio runner conflicts; real-browser tests now cross the live Python stack without Ollama.
- 2026-09-08 (frontend/deployment): Docker daemon is unavailable and the CLI lacks Compose here; production assets are exercised through live FastAPI, but the new multi-stage container build could not be executed locally.

- 2026-08-27 (M0/P1/P4): Git contained no recoverable pre-framework source, so the concrete control had to be reconstructed and frozen under `baseline/hand_rolled/`; comparisons explicitly use that artifact rather than a remembered implementation.
- 2026-08-27 (M0/P5): Current Pydantic AI 2.35 exposes MCP at `pydantic_ai.mcp` and AG-UI at `pydantic_ai.ui.ag_ui`, not the paths assumed by the plan; framework source inspection was needed to find the moved APIs.
- 2026-08-27 (M0/P2): `pydantic-ai-slim[mcp]` installs only FastMCP client support; an in-process server fails at import until full `fastmcp` (or its server extra) is added.
- 2026-08-27 (M1/P2): `MCPToolset(FastMCP)` made in-memory discovery and calls native to an `Agent` run with no transport process, but union return values arrive through the direct FastMCP client as a structured `result` root and need explicit typed unwrapping outside Pydantic AI.
- 2026-08-27 (M1/P5): PGlite socket accepts asyncpg query traffic but its COPY path loses protocol synchronization and asyncpg's graceful close waits indefinitely; deterministic tests use typed CSV inserts and terminate each test connection. This is test-harness/transport friction, not MCP behavior.
- 2026-08-27 (M1/P1): The deterministic walking slice needed one `Agent`, one filtered MCP toolset, and no hand-written tool dispatch; the control needs an explicit request loop, wire-message construction, retry limit, and tool-result plumbing.
- 2026-08-27 (M2/P1): Frozen output/query variants make truncation and rejection assertions direct and prevent status-string branching; provider final output is validated as `AgentAnswer`, at the cost of an output-tool round trip for tool-output mode.
- 2026-08-27 (M2/P2): Tool exceptions were less useful than typed safe rejection values for keeping backend details out of history. FastMCP schemas preserve the variants, while database error sanitization remains application code as expected.
- 2026-08-27 (M2/P5): `AgentRunResult.usage` is now a property rather than the older callable shape; source/signature inspection corrected the measurement seam.
- 2026-08-27 (M3/P2/P8): Native `FilteredToolset(MCPToolset(...))` made the three exposure shapes one small builder; prefetch still needed a separate FastMCP client call plus typed result parsing, the clearest point where in-process MCP felt bolted on.
- 2026-08-27 (M3/P8): The final real-Ollama v1 matrix had 18/18 correct runs and all safety/held-out checks green. Prefetched won lexicographically: 6 correct, 2.5 request macro-median, 14.229s latency, and 1654.5 input tokens versus catalog 3.5/15.629s/2197.5 and granular 5.5/20.872s/3720.
- 2026-08-27 (M3/P7): An early workload wording (“completed journeys”) caused repeat timeouts despite an equivalent direct smoke succeeding; making the v1 prompt concrete before freezing the artifact produced 18/18. Local-model reliability is sensitive to wording even when schema/tool correctness is unchanged.
- 2026-08-27 (M4/P3): The route still ends in one `AGUIAdapter.dispatch_request` call, but real behavior adds per-request catalog prefetch, request deps, model settings, filtered toolsets, and an `on_complete` bridge for structured output text/usage. The front door stayed recognizable, not literally one line of behavior.
- 2026-08-27 (M4/P4): The same real journey succeeded through both controls. The final artifact has AG-UI at 23 events across 9 observed types in 7.200s and raw SSE at 12 events across 4 observed types in 19.142s. The event count is vocabulary evidence, not a protocol-speed claim.
- 2026-08-27 (M4/P4/P5): Reconstructing valid AG-UI history required grouping tool calls by parent message, echoing call/result pairs, and preserving encrypted continuity events. Missing a call on an existing assistant message deterministically broke the next turn; source inspection of the adapter was needed to understand the wire invariant.
- 2026-08-27 (M4/P5/P7): Gemma reasoning exposed an AG-UI stream-state failure (`handle_thinking_start must be called before handle_thinking_delta`). Ollama's OpenAI endpoint ignored `think:false`; `reasoning_effort: none` suppressed reasoning reliably enough for the accepted journey and is now explicit app configuration.
- 2026-08-27 (M4/P6): Full resend met the probe's declared turn-10 gate: 4223/262144 input tokens (1.61%), 12.867s versus 6.747s at turn 1 (1.91×), and a correct answer. Turn 2 still failed and turn 8 took 105.87s, so “acceptable” is only the narrow threshold verdict, not a reliability endorsement.
- 2026-08-27 (M4/P6): `ProcessHistory` wiring was one capability argument plus a 34-line immutable pair-preserving processor. It cut serialized model history from 28,584 to 11,957 bytes and preserved correctness, but total turn input rose from 4,223 to 4,771 tokens and latency reached 132.5s because the model needed more work; compaction saved payload, not this run's cost.
- 2026-08-27 (M5/P7): The agent image built and a containerized agent + seeded Postgres acceptance path passed against the exact host Ollama model after one observed flaky AG-UI run. The installed Docker CLI has no Compose plugin and the 0.32.15 Ollama image pull stalled twice, so no claim is made that the checked-in full stack was executed here.
- 2026-08-27 (M5/P7): Gemma completed core, AG-UI, baseline, held-out, and hybrid-composed journeys; no SQL-only fallback was needed. Redundant discovery/query calls and latency outliers remain the practical local-model cost.
- 2026-08-27 (review/P4): Independent Terra review found that completion bridging duplicated a real answer and that browser argument accumulation always targeted the first parallel call. RED regressions now suppress the bridge when matching text already streamed and select argument targets by call ID.
- 2026-08-27 (review/safety): Independent review found the output cap was applied after `fetch()` materialized every row. A slow-row RED test now proves the asyncpg cursor stops after `row_cap + 1`, making truncation a resource cap rather than only a response cap.
- 2026-08-27 (final review/safety): Terra found that read-only SQL under Compose's bootstrap superuser still allowed privileged file/program capabilities. RED tests now reject non-query statement families and known server-file/admin functions before execution, while Compose provisions a dedicated `NOSUPERUSER` SELECT-only application role.
- 2026-08-27 (final review/resource): Terra found the app-scoped MCP trace retained every result indefinitely. `create_app` now explicitly disables and clears call recording; traces remain enabled only for bounded acceptance/experiment runs.
- 2026-08-27 (security re-gate): Terra found top-level keyword checks missed data-modifying CTEs and `EXPLAIN ANALYZE` writes. An unreachable-DSN RED test now proves recursive DML/DDL token scanning rejects both before any connection attempt.
- 2026-09-02 (post-verdict/P2): FastMCP 4.0.1 source inspection and an isolated MCP SDK v2 run passed all 58 default tests with one camelCase warning; modern MCP removes handshake/session affinity, but Pydantic AI's in-memory path still crosses client discovery, tool dispatch, and the structured-result wrapper.
- 2026-09-02 (post-verdict/P2/P8): Cloudflare Code Mode, Agent Skills, MCPorter, and Pydantic AI deferred-tool source/docs review separated four often-conflated ideas: server-side Code Mode can retain MCP, Skills need an execution surface, CLI bridges can retain MCP underneath, and `.defer_loading()` is the transparent schema-context ablation; none targets this app's current one-tool prefetched surface strongly enough to justify a host shell or code sandbox.
- 2026-09-04 (application/MCP): FastMCP 4.0.2 and Pydantic AI source/signature inspection confirmed that `Client(server, mode="auto")` negotiates modern MCP v2 (`server/discover`, currently `2026-07-28`) with no legacy initialize result; the production toolset now uses that path explicitly.
- 2026-09-04 (application/simplification): The experiment-only exposure policy, tracing, PGlite runner, metrics, and workloads moved under `sql_agent.benchmark`; production now has one catalog/query tool surface and no exposure-mode configuration or retained call trace.
- 2026-09-04 (application/MCP): FastMCP 4 modern structured results expose generated dataclass instances through `CallToolResult.data`; benchmark/test decoding normalizes that boundary with `dataclasses.asdict` instead of leaking generated result classes into domain code.
- 2026-09-04 (application/exposure): Production now uses the benchmark-winning prefetched shape: `get_catalog` runs over modern MCP v2 before every agent run, its typed result is injected as run instructions, and the model receives only `run_query`.
- 2026-09-04 (application/performance): A real six-run production-path check on the same two-case workload and exact model digest was 6/6 correct; versus the recorded prefetched winner, request macro-median was unchanged at 2.5, latency was 14.703s vs 14.229s (+3.33%), input was 1719.5 vs 1654.5 tokens (+3.93%), and first event improved to 2.388s vs 2.680s (-10.89%).
- 2026-09-04 (application/AG-UI): Real frontend replay exposed that AG-UI streams plain-text attempts rejected by typed output validation, so one Gemma run rendered four to five answers and leaked evidence before completion; the app now buffers response text, emits only the final validated answer, and hides the internal output tool while retaining database-tool lifecycle events.
- 2026-09-10 (benchmark/Ollama): Ollama 0.32.15 ignores `options.num_ctx` on `/v1/chat/completions` (the model loaded at the 4096 server default) while `/api/chat` honours it; `/api/create` with `from` + `parameters.num_ctx` yields a weight-sharing tag whose context the OpenAI route respects, verifiable via `context_length` in `/api/ps`. `reasoning_effort` low/medium/high all enable Gemma 4 thinking through the same route; the sweep uses derived tags for context and per-request settings for everything else.
- 2026-09-10 (benchmark/pydantic-ai): `UsageLimitExceeded` is an `AgentRunError`, not `UnexpectedModelBehavior`, so the demo grader now records it as `request_limit`; per-request input tokens come from `ModelResponse.usage` in `all_messages()` and drive the context-pressure flag.
- 2026-09-10 (application/Ollama): The first parameter sweep showed 501 output tokens under a 256 cap. Pydantic AI's OpenAI chat model sends `max_completion_tokens` by default and Ollama 0.32.15 ignores it (only legacy `max_tokens` caps generation; verified by direct request), so `SQL_AGENT_MAX_OUTPUT_TOKENS` had never applied. `ollama_model` now merges `OpenAIModelProfile(openai_chat_supports_max_completion_tokens=False)`, pinned by a mock-transport unit test on the request body.
- 2026-09-10 (benchmark/PGlite): The corrected sweep killed its PGlite database after ten failed statements. Wire inspection (`PGLITE_INSPECT=1`) shows PGlite answering asyncpg's Flush after an ErrorResponse with a ReadyForQuery that real Postgres only sends after Sync; the duplicate leaves a stray message that desyncs the next statement on that connection (`'NoneType' object has no attribute 'decode'` on ROLLBACK) and makes `terminate()` reset the socket. pglite-socket 0.2.11 never removes a handler that detaches through its error path, so each reset consumed one of `maxConnections=10`. `pglite_manager.js` now re-emits the close event on handler errors, and `run_query` no longer issues ROLLBACK on a connection it is about to terminate, so the model receives a typed rejection instead of a tool traceback.
- 2026-09-10 (benchmark/diagnostics): Failed demo runs kept no queries, so the expert case's repeated failures were opaque. `run_case` now owns the benchmark trace and stores attempted queries on `EvaluationFailed`; the first expert trace showed a 5 s statement timeout on a correlated subquery, a query cut mid-statement by the 512-token cap, then a correct result whose final answer never validated. The sweep gained a `statement_timeout_ms` axis and the sweep now aborts if the database stops answering.
- 2026-09-10 (benchmark/pydantic): The harness killed the sweep for low memory; resuming from the checkpoint silently flipped nine earlier `result_incorrect` records to `result_correct`. `ResultCorrect` and `ResultIncorrect` share one shape and their `kind` tags were `init=False`, which pydantic ignores on validation, so the undiscriminated union always chose the first member. `DemoOutcome` is now `Annotated[..., Field(discriminator="kind")]` with ordinary defaulted tags, pinned by a round-trip test; the affected records were regraded from their stored rows.
